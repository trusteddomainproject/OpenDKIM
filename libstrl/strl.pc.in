# Process this file with autoconf to produce a pkg-config metadata file.

prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@

Name: STRL (Safe String Copy/Concatenate Library)
Description: Library containing strlcat() and strlcpy() for systems without it
URL: http://www.opendkim.org
Version: @VERSION@
Libs: -L${libdir} -lstrl
Cflags: -I${includedir}

